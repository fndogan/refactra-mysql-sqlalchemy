You are assisting with a manually reviewed Python database migration.

Convert the supplied function to SQLAlchemy without trusting dynamic table or
column identifiers. Prefer SQLAlchemy expressions and named bind parameters,
preserve the function's observable behavior, and return only proposed Python
code for human review.

This packaged prompt is a general example. Projects should provide their own
reviewed instructions when they require application-specific conventions.
